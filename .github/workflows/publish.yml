name: Release & Publish

on:
  push:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get package version
        id: package
        run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "v${{ steps.package.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if GitHub release exists
        id: check_release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "v${{ steps.package.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if npm version is published
        id: check_npm
        run: |
          if npm view "blankdrive@${{ steps.package.outputs.version }}" version >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Evaluate release work
        id: work
        run: |
          if [ "${{ steps.check_release.outputs.exists }}" = "false" ] || [ "${{ steps.check_npm.outputs.exists }}" = "false" ]; then
            echo "required=true" >> $GITHUB_OUTPUT
          else
            echo "required=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Node.js
        if: steps.work.outputs.required == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        if: steps.work.outputs.required == 'true'
        run: npm ci

      - name: Build
        if: steps.work.outputs.required == 'true'
        run: npm run build

      - name: Run tests
        if: steps.work.outputs.required == 'true'
        run: npm run test -- --run

      - name: Create and push tag
        if: steps.check_tag.outputs.exists == 'false' && steps.check_release.outputs.exists == 'false'
        run: |
          git tag "v${{ steps.package.outputs.version }}"
          git push origin "v${{ steps.package.outputs.version }}"

      - name: Generate release notes
        if: steps.check_release.outputs.exists == 'false'
        run: |
          set -euo pipefail

          VERSION="${{ steps.package.outputs.version }}"
          CURRENT_TAG="v$VERSION"
          PREV_TAG="$(git tag --sort=-version:refname | grep -v "^${CURRENT_TAG}$" | head -n 1 || true)"
          if [ -n "$PREV_TAG" ]; then
            RANGE="$PREV_TAG..HEAD"
          else
            RANGE="HEAD"
          fi

          git log "$RANGE" --pretty=format:"%s" --no-merges > .release_commits.txt || true

          {
            echo "# BlankDrive v$VERSION"
            echo
            if [ -n "$PREV_TAG" ]; then
              echo "_Changes since $PREV_TAG._"
            else
              echo "_Initial release notes._"
            fi
            echo
            echo "## Patch Notes"
          } > RELEASE_NOTES.md

          PATCH_COUNT=0
          while IFS= read -r subject; do
            if [[ "$subject" =~ ^(fix|hotfix|perf|refactor|security|patch)(\(.+\))?:[[:space:]] ]]; then
              echo "- $subject" >> RELEASE_NOTES.md
              PATCH_COUNT=$((PATCH_COUNT + 1))
            fi
          done < .release_commits.txt

          if [ "$PATCH_COUNT" -eq 0 ]; then
            echo "- No patch-level fixes recorded in this release window." >> RELEASE_NOTES.md
          fi

          echo >> RELEASE_NOTES.md
          echo "## Update Notes" >> RELEASE_NOTES.md

          UPDATE_COUNT=0
          while IFS= read -r subject; do
            if [[ "$subject" =~ ^(feat|docs|chore|build|ci|test|style|release)(\(.+\))?:[[:space:]] ]]; then
              echo "- $subject" >> RELEASE_NOTES.md
              UPDATE_COUNT=$((UPDATE_COUNT + 1))
            fi
          done < .release_commits.txt

          if [ "$UPDATE_COUNT" -eq 0 ]; then
            while IFS= read -r subject; do
              if [ -n "$subject" ]; then
                echo "- $subject" >> RELEASE_NOTES.md
                UPDATE_COUNT=$((UPDATE_COUNT + 1))
                if [ "$UPDATE_COUNT" -ge 10 ]; then
                  break
                fi
              fi
            done < .release_commits.txt
          fi

          if [ "$UPDATE_COUNT" -eq 0 ]; then
            echo "- No additional updates recorded in this release window." >> RELEASE_NOTES.md
          fi

      - name: Create GitHub Release
        if: steps.check_release.outputs.exists == 'false'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.package.outputs.version }}
          name: v${{ steps.package.outputs.version }}
          body_path: RELEASE_NOTES.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to npm
        if: steps.check_npm.outputs.exists == 'false'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Skip (already published and released)
        if: steps.work.outputs.required == 'false'
        run: echo "Version v${{ steps.package.outputs.version }} is already published on npm and has a GitHub release."

      - name: Release summary
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.package.outputs.version }}"
          TAG="v${VERSION}"

          RELEASE_URL="$(gh release view "${TAG}" --json url --jq '.url' 2>/dev/null || true)"
          NPM_VERSION_PUBLISHED="$(npm view "blankdrive@${VERSION}" version 2>/dev/null || true)"
          NPM_LATEST="$(npm view blankdrive dist-tags.latest 2>/dev/null || true)"

          if [ -n "${RELEASE_URL}" ]; then
            if [ "${{ steps.check_release.outputs.exists }}" = "false" ]; then
              RELEASE_STATUS="created in this run"
            else
              RELEASE_STATUS="already existed"
            fi
          else
            RELEASE_STATUS="missing after run"
          fi

          if [ "${NPM_VERSION_PUBLISHED}" = "${VERSION}" ]; then
            if [ "${{ steps.check_npm.outputs.exists }}" = "false" ]; then
              NPM_STATUS="published in this run"
            else
              NPM_STATUS="already existed"
            fi
          else
            NPM_STATUS="missing after run"
          fi

          {
            echo "## Release Summary"
            echo
            echo "- Version: \`${VERSION}\`"
            echo "- Tag exists: \`${{ steps.check_tag.outputs.exists }}\`"
            echo "- GitHub release status: ${RELEASE_STATUS}"
            if [ -n "${RELEASE_URL}" ]; then
              echo "- GitHub release URL: ${RELEASE_URL}"
            fi
            echo "- npm publish status: ${NPM_STATUS}"
            if [ -n "${NPM_LATEST}" ]; then
              echo "- npm dist-tag latest: \`${NPM_LATEST}\`"
            fi
          } >> "${GITHUB_STEP_SUMMARY}"
